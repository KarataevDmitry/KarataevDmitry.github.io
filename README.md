# KarataevDmitry.github.io

GitHub Pages site: **Settings → Pages** uses the **`docs/`** folder on **`main`**. The live site root is the contents of `docs/` — edit files there (or push to `main`) to update https://karataevdmitry.github.io/ .
